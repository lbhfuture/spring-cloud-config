zuul:
  routes:
    - path: '/user/**'
      retryable: 'true'
      service-id: 'provider-user'
      strip-prefix: 'false'
    - path: '/weisi/**'
      retryable: 'true'
      service-id: 'provider-weisi'
      strip-prefix: 'false'
